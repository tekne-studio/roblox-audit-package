name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
  workflow_run:
    workflows: ["Auto Release"]
    types:
      - completed

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    # Only run if triggered by tag push, manual dispatch, or successful auto-release
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    permissions:
      contents: write  # Required for creating releases

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create release archives
        run: |
          mkdir -p dist

          # Since these are pure Lua scripts, they're platform-independent
          # Create a single universal package with rokit.toml manifest

          # Universal package (works on all platforms)
          mkdir -p roblox-audit
          cp rokit.toml roblox-audit/
          cp -r src roblox-audit/

          # Create compressed archives
          tar -czf dist/roblox-audit.tar.gz roblox-audit
          zip -r dist/roblox-audit.zip roblox-audit

      - name: Extract version
        id: get_version
        run: |
          # Try to get version from tag first (manual or tag push trigger)
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
          else
            # Get version from rokit.toml (workflow_run trigger)
            VERSION=$(grep '^version = ' rokit.toml | cut -d'"' -f2)
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release v${{ steps.get_version.outputs.VERSION }}
          body: |
            ## Roblox Audit Tools v${{ steps.get_version.outputs.VERSION }}

            ### Installation

            ```bash
            rokit add tekne-studio/roblox-audit@${{ steps.get_version.outputs.VERSION }}
            ```

            ### What's included

            - 🔍 **analyze-dependencies** - Detect circular dependencies
            - 🎨 **visualize-dependencies** - Generate dependency graphs
            - 🛡️ **audit** - Comprehensive code quality checks (auto-initializes configs)

            See [README.md](https://github.com/${{ github.repository }}/blob/main/README.md) for full documentation.
          files: |
            dist/*.tar.gz
            dist/*.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
